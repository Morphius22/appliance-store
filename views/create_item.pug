extends layout

block content
  h1=title

  form(method='POST' action='')
    .form-control.w-full.max-w-xs
        label.label
            span.label-text Item Name
        input.input.input-bordered.w-full.max-w-xs(type='text' placeholder='Type here')
        label.label
            span.label-text Item Description
        input.input.input-bordered.w-full.max-w-xs(type='text' placeholder='Type here')
        label.label
            span.label-text Price Per Unit
        input.input.input-bordered.w-full.max-w-xs(type='text' placeholder='Type here')
        label.label
            span.label-text Number in Stock
        input.input.input-bordered.w-full.max-w-xs(type='text' placeholder='Type here')
        label.label
        span.label-text What Category Is The Item In?
        select.select.select-bordered
        option(disabled='' selected='') Pick one
        option Refrigerators
        option Stoves
        option Ovens
    button.btn.btn-primary(type='submit') Submit